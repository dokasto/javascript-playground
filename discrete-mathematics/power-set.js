/**
 * Find all subset of a set
 * Power set 2^n, where n = number of items in the set
 */

 const powerSet = array => {
	 const sets = [];

	function helper(givenArray, subset, index) {
		if(givenArray.length === index) {
			sets.push(subset);
		}

		
	}


 };